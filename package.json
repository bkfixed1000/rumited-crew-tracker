{
  "name": "rumited-crew-tracker",
  "version": "1.3.2",
  "private": true,
  "description": "Runner whitelist viewer with refresh + flash, using Node 18 global fetch (no node-fetch)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "express": "^4.18.2"
  }
}